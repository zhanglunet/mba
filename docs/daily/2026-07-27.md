# 日报 · 2026-07-27

> 自动生成:盘点 2026-07-27(Asia/Shanghai)合并到 main 的提交 / PR(`scripts/new_daily.py`)。「工作盘点」由 git 派生、可验证;「补充说明」留人工。

## 工作盘点(5 项)

### 功能(2)
- 第一场创始人×投资人晚餐 —— 王慧文 × 徐新(美团 × 今日资本) `collabs` · [#218](https://github.com/zhanglunet/mba/pull/218)
- 新增投资人维度 + 晚餐支持创始人×投资人配对(方案 A) `investors` · [#217](https://github.com/zhanglunet/mba/pull/217)

### 文档(1)
- 日报 2026-07-26 [skip ci] `daily`

### 杂项(1)
- 每日自动发现候选事件 2026-07-27（1 条,前台 triage 待核验） `watch`

### 其他(1)
- docs+feat: RSSHub 部署手册(docs/27) + 徐新进 consumer-cn 面板、语料补第三个一手源、评委名单加中文姓名 · [#215](https://github.com/zhanglunet/mba/pull/215)

## 补充说明

**今日主线**:一句「增加一个评委:徐新,今日资本的」——**核实后她本来就在**,于是这一天做的
全是由此牵出的真实缺口:面板归属、语料刷新、名单可读性,最后长出一个**新维度**。

### 先核实再动手,省下一整套重复工

用户说"增加评委徐新"。动手前先查:`perspectives/xuxin-perspective/` **已存在且是 full tier**
(6 路调研齐全)、在 `vc-cn` 面板、`quality_check` 6/6、线上 API 可查,2026-07-07 就深化完了。
**如果直接开做,就是把一套已有的 full tier 重做一遍。**

有意思的是:我在确认她是否存在**之前**已经去抓了一手语料,抓到的内容**和已入库的互相印证**
(南大访谈里的 winner-pattern 三段论、"相信品牌的力量、相信复合增长的力量"、密集开店 20%/50%
正好对应她档案里的模型 4/5/1)—— 等于顺手给现有档案做了一次独立交叉验证。

### 关键决策

- **中文名不新建真源,复用 panel 已有字段**:评委名单只有 slug 与 `xuxin-perspective` 这类内部名,
  中文读者看不出是谁。但中文名**早就在 `panels/*.yaml` 的 `display_name_cn` 里且 43/43 全覆盖** ——
  直接取用,**不新建一份会漂移的手写名单**。配 `check_consistency` 第 14 格守着。
- **投资人单开一个维度,不塞进 founders**(方案 A):`founders/` 是**按品牌建索引**的
  (`brand ∈ published-reports.txt` 且文件名 == brand)。徐新是投资人不隶属任何被审品牌,
  硬塞只有两条路 —— **伪造 brand(造假)** 或放宽 founders 允许空 brand(把"创始人维度"语义搞混)。
  故 `investors/` **按人建索引**(slug == perspective slug)。
- **「徐新 × 刘强东」做不了,换有真实投资关系的配对**:京东不在被审品牌白名单、没有创始人档案。
  改用**王慧文 × 徐新(美团)** —— 她在南大访谈里**亲口把京东与美团并列为最成功的两个案例**,
  素材有据,不是将就。
- **诚实盒写满 5 条而不是凑数**:这一场「品类聚焦 vs 无边界」是**真实分歧**,明写**桌上没谈拢**;
  另标"给到远超开口的钱在补贴战里可能反而拖长窗口期"、"20%/50% 阈值迁到线上是推演非结论"、
  "王慧文已离任不代表美团现行立场"、"徐新与美团有历史投资关系应打折"。

### 踩坑(三个)

1. **schema 的语义边界是设计约束,不是障碍** —— 一开始想直接给徐新建 `founders/xuxin.yaml`,
   被校验器的"brand 必须在白名单"挡住。**这个挡是对的**:它逼出了"按品牌 vs 按人"这个
   真正的建模问题,而不是让我糊一个假 brand 混过去。
2. **放宽了校验器,却差点没验证"真的放宽了"** —— `validate_collabs` 从 `founder_exists` 改成
   `participant_exists` 后 selftest 仍然 16 组全绿,但**没有任何一条断言覆盖投资人路径**。
   补了一条:用真实的 `investors/*` + `founders/*` 各取一个组一场,**必须不报"缺档案"**。
   教训:**放宽约束时,要加一条"新路径确实通"的断言**,否则改动可能是空的。
3. **渲染侧的 slug 兜底会把内部名当品牌名显示** —— 投资人第一次上桌,chip 和发言气泡都显示成
   「徐新 **xuxin**」(`names.get(slug, slug)` 回落)。而且**不止一处**:chip 副标题、气泡标签、
   首页亮点、页面 description 共 5 处。→ 抽出 `party_label()` 统一(创始人=品牌名、投资人=机构名)。
   教训:**回落值(fallback)在新场景下会变成 bug**;发现一处要顺藤摸完全部调用点。

### 今日新增门禁 / 数字

- `check_consistency` **13 → 15 格**(新增「评委中文名」「投资人维度」),两格都做了 red-green。
- 新增 `validate_investors.py` **12 组自测**;`validate_collabs` 补 1 条投资人路径断言。
- consumer-cn 面板 5 → 6 人;晚餐 3 → 4 场;docs 27 → 28 篇。

### Superpowers 自检(以 `docs/25` checklist)

通过。**§0「先查现状再动手」这一轮直接省下一整套重复工**(徐新已存在)——
这是今天最大的单项收益,而它来自**动手前先花两分钟核实**。
**§2 verification**:晚餐页用 Playwright 实看,抓出 chip 与气泡的 slug 兜底问题 ——
`validate_collabs` 全绿但页面显示是错的,**门禁绿 ≠ 产物对**。
**偏离**:§3 未出正式 plan;§6 未派 reviewer subagent(改动面清晰,合理简化)。

### 明日计划

- 用户本地装 Docker → RSSHub 验微博路由(`docs/27`),通了填 `rss_feeds` 即开吃。
- 观察官方源上线后的流水线:Opus 5 官方公告是否以 P0 入库并触发 anthropic 重审。
- (可选)扩投资人维度:vc-cn 另外四位都已有 full tier perspective,边际成本低;
  下一场晚餐候选 **张磊 × 钟睒睒**(高瓴投过农夫山泉,都信长期主义但路径相反)。

---
提交:5 · PR:3
