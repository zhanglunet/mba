# Wuying Helpers

Helpers for creating and smoke-testing an AgentBay/Wuying browser session.

Run from the repo root:

```bash
python3 scripts/wuying/smoke_test.py
python3 scripts/wuying/open.py
```

Both scripts read `WUYING_API_KEY` and optional `WUYING_IMAGE_ID` from the repo
root `.env`.
