# MBA 发布白名单 / Publish whitelist
#
# 每行一个 brand-slug,对应 metric-brand-auditor/reports/<slug>/report.html
# build.sh 只把这里列出的报告拷到 site/reports/<slug>/index.html 上线。
#
# 没列在这里的报告 —— 即使本地存在 —— 不会被发布。
# .gitignore 仍然 ignore 整个 reports/ 目录,所以发布前必须显式:
#
#   git add -f metric-brand-auditor/reports/<slug>/report.html
#   git add site/published-reports.txt
#   git commit && git push
#
# 取消发布只需删除这一行并删掉 site/reports/<slug>/(下次 build 不会再生)。
#
# 用 # 注释暂停某行,留作占位。
#
# ---- 列表 ----
# (暂无,首发只上首页)
